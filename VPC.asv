clc;
clear all;
close all;

%ryzesetup Configurar Tello por primera vez
list=ryzelist %Información básica de los drones conectados

r=ryze() %Nuestro objeto
takeoff(r); %La dvd no entendí para que

move(r,[.5 0 0, 'Speed', .5])



land(r);